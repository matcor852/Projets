Implémentation d'un chiffrement xor simple en python.
Prend en entrée une chaine de caractères non chiffrée (plaintext) et une clé (key), applique 
un chiffrement xor depuis la clé puis déchiffre avec cette même clé.
